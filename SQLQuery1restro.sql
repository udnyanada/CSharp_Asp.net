use restro;

create table item(
id int identity(1,1) primary key,
namei varchar(250) not null,
category varchar(250) not null,
price bigint not null);

namei,category,price

select * from item;

